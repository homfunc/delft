# Core scientific stack (Py 3.11 compatible)
numpy
pandas
scikit-learn
h5py
regex
tqdm

# Deep learning: Keras 3 API (backend-agnostic)
keras>=3.0.0

# Optional backends you can enable as needed:
# - TensorFlow (required for ELMo-dependent models)
tensorflow>=2.18
# - PyTorch backend for testing
# torch>=2.3
# - JAX backend for testing
# jax>=0.4.30
# jaxlib>=0.4.30

# KerasHub (use instead of HuggingFace Transformers)
keras-hub>=0.2.0
# load weights/artifacts from the Hugging Face Hub without Transformers
huggingface_hub>=0.23.0
safetensors>=0.4.0

# Local editable install of keras3-crf during development
# -e ../keras-crf

lmdb
truecase
requests
pytest

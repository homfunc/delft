# Core scientific stack (Py 3.11 compatible)
numpy
pandas
scikit-learn
h5py
regex
tqdm

# Deep learning backends and APIs
# Keras 3 API (backend-agnostic). Optional TF backend if you choose TensorFlow.
keras>=3.0.0
# Optional: TensorFlow backend (remove if using JAX/PyTorch exclusively)
tensorflow>=2.18

# KerasHub for HF integration without transformers
keras-hub>=0.2.0
huggingface_hub>=0.23.0
safetensors>=0.4.0

# Local editable install of keras3-crf during development
-e ../keras-crf

lmdb
truecase
requests
pytest
